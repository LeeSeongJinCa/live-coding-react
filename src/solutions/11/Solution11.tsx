/**
 * 11. Create a Real-Time Search Filter
 *
 * Problem:
 * - Create a search filter component that filters a list of items in real-time as the user types.
 * - 사용자가 입력하는 대로 실시간으로 항목 목록을 필터링하는 검색 필터 컴포넌트를 만듭니다.
 */

export const Solution11 = () => {
  // 5번 문제 (Create a Search Bar) 와 동일한 문제라서 패스합니다.
  return <div>5번 문제 (Create a Search Bar) 와 동일한 문제라서 패스합니다.</div>;
};
